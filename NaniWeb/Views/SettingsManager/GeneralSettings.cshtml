@model GeneralForm

@{
    ViewData["Title"] = "General - Settings";
}

<partial name="_AdminNav"/>

<div class="container mt-5">
    <div class="card bg-primary border-secondary">
        <h5 class="card-header bg-primary">Settings</h5>
        <div class="card-body">
            @if (TempData["Error"] != null && (bool) TempData["Error"])
            {
                <div class="alert alert-dismissible alert-danger">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                    Something went wrong. Please check your data and try again.
                </div>
            }
            else if (TempData["Error"] != null && !(bool) TempData["Error"])
            {
                <div class="alert alert-dismissible alert-success">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                    Settings saved successfully.
                </div>
            }
            <form id="general_settings_form" asp-controller="SettingsManager" asp-action="General" novalidate>
                <fieldset>
                    <legend>General settings</legend>
                    <div class="form-group">
                        <div class="form-check">
                            <label class="form-check-label">
                                <input class="form-check-input" aria-describedby="enable_registration_help" id="enable_registration" asp-for="EnableRegistration"/>Enable registration
                            </label>
                        </div>
                        <small id="enable_registration_help" class="form-text text-muted">Whether you want to allow new users to register to this website.</small>
                    </div>
                    <div class="form-group">
                        <label for="site_name">Site name</label>
                        <input class="form-control" id="site_name" asp-for="SiteName" aria-describedby="site_name_help" placeholder="Site name">
                        <small id="site_name_help" class="form-text text-muted">The name of your website.</small>
                    </div>
                    <div class="form-group">
                        <label for="site_description">Site description</label>
                        <input class="form-control" id="site_description" asp-for="SiteDescription" aria-describedby="site_description_help" placeholder="Site description">
                        <small id="site_description_help" class="form-text text-muted">The description of your website. This will appear in search engines.</small>
                    </div>
                    <div class="form-group">
                        <label for="site_url">Site URL</label>
                        <input class="form-control" id="site_url" asp-for="SiteUrl" aria-describedby="site_url_help" placeholder="Email address">
                        <small id="site_url_help" class="form-text text-muted">Your website's url without trailing slashes. ex: https://google.com, https://mangadex.org</small>
                    </div>
                    <div class="form-group">
                        <label for="site_about">About page</label>
                        <textarea class="form-control" id="site_about" asp-for="SiteAboutPage" aria-describedby="site_about_help" placeholder="About" rows="10"></textarea>
                        <small id="site_about_help" class="form-text text-muted">The code for your site's about page. In HTML.</small>
                    </div>
                    <div class="form-group">
                        <label for="site_site_panel">Side panel</label>
                        <textarea class="form-control" id="site_site_panel" asp-for="SiteSideBar" aria-describedby="site_site_panel_help" placeholder="Side panel" rows="10"></textarea>
                        <small id="site_site_panel_help" class="form-text text-muted">The code for your site's side panel. In HTML.</small>
                    </div>
                    <div class="form-group">
                        <label for="site_footer">Site footer</label>
                        <textarea class="form-control" id="site_footer" asp-for="SiteFooter" aria-describedby="site_footer_help" placeholder="Footer" rows="10"></textarea>
                        <small id="site_footer_help" class="form-text text-muted">The code for your site's footer. In HTML.</small>
                    </div>
                </fieldset>
                <div>
                    <button class="btn btn-success float-right mt-3" id="submit" type="submit">Save settings</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Body
{
    <script>
        $(function() {
            $("#settings").addClass("active");
        });
    </script>
}