@inject SettingsKeeper SettingsKeeper

@if (bool.Parse(SettingsKeeper.GetSetting("EnableAds").Value) && SettingsKeeper.GetSetting("AdsBodyScripts").Value != string.Empty)
{
    @Html.Raw(SettingsKeeper.GetSetting("AdsBodyScripts").Value)
}