@inject SettingsManager SettingsManager

@if (!string.IsNullOrWhiteSpace(SettingsManager.GetSetting("SiteFooterCode").Value))
{
    <footer class="footer bg-secondary">
        <div class="container">
            <div class="text-center">
                @Html.Raw(SettingsManager.GetSetting("SiteFooterCode").Value)
            </div>
        </div>
    </footer>
}
else
{
    <footer class="footer bg-secondary">
        <div class="container">
            <div class="text-center">
                A wild footer appeared. Change it in the admin panel.
            </div>
        </div>
    </footer>
}